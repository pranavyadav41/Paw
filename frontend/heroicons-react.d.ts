declare module '@heroicons/react/solid' {
    import { ComponentType } from 'react';
  
    export const StarIcon: ComponentType<any>; // Or use specific props if available
    // Add declarations for other icons as needed
  }
  